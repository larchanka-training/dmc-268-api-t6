[project]
name = "auth-api"
version = "0.1.0"
description = "Authentication and identity HTTP API"
requires-python = "==3.13.*"
dependencies = [
    "fastapi>=0.118.0",
    "uvicorn[standard]>=0.37.0",
]
