# DMC-268 API (Team 6)

FastAPI backend service for DMC-268 Team 6.

## Setup & Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload
```
