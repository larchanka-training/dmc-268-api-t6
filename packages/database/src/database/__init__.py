"""Shared PostgreSQL persistence package.

The package deliberately exposes neither HTTP nor messaging concerns. Services use
it through their infrastructure layer and own their application ports.
"""
